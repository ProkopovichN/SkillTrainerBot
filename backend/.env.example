BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PUSH_URL=http://localhost:8080/push   # если фронт слушает 8080
FRONTEND_PUSH_TOKEN=  # если фронт требует токен, заполни тем же значением что в фронтовом PUSH_TOKEN
REMINDER_DELAY_SECONDS=300
OPENROUTER_API_KEY=  # опционально, если хочешь AI-оценку кейсов
OPENROUTER_MODEL=x-ai/grok-4.1-fast   # опционально